new:
  base_url: "http://localhost:10104"
  datastores:
    - { name: default, type: mysql, host: 127.0.0.1, port: 13307, user: root, password: root }
old:
  base_url: "http://localhost:10103"
  datastores:
    - { name: default, type: mysql, host: 127.0.0.1, port: 13306, user: root, password: root }
attribution: { quiet_ms: 300, timeout_ms: 5000 }
noise:
  response_paths: ["**.updated_at", "**.created_at"]
  columns: ["*.updated_at", "*.created_at"]
compare:
  sort_arrays: [{ path: "contracts", by: "contract_id" }]
report: { dir: "./report" }
corpus: { dir: "./corpus" }
