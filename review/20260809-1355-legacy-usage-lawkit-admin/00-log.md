# 팀 작업 로그 — legacy 실사용 검증 + lawkit-admin 흡수

**사용자 원문 요청**
> 일단 제일 쉬운건 lawkit-admin 을 흡수시키는거고 그 다음에 레거시 서비스는 실제 호출되는게 몇개나 되나, 110여개를 다 쓰나? 셀프 리뷰로 검증해봐 로킷 어드민은 합쳐

**구성**: writer=claude · reviewer=claude(셀프리뷰) · mode=review · rounds=2 · Codex 미사용

## 13:55 오케스트레이터 → legacy 실사용 선실측 (운영 26h 캡처)
- `/api/v1` 총 241건(OPTIONS 115 포함) → **실호출 126건 / 고유 엔드포인트 21개**
- legacy(medilawyer-boot) 구현 엔드포인트 **147개** → **실사용 21개 = 14%**
- 즉 "110여개를 다 쓰나?" 답: **아니다. 26시간 창에서 14%만 쓰였다.**
- 실사용 21개 성격: 리뷰부스터 v2 발송/템플릿(boost1), 사용자·스토어 조회, 팀 통계(presentHistories·averageScore·keyword 등), AI 답글 추천, 채널계정 로그인
- 주의: 26시간 창이라 월간/주간 배치성 호출은 누락 가능 → 보고에 한계 명시

## 14:20 사용자 → 오케스트레이터 (중요 제약)
- "여기는 트래픽이 작은 회사라서 26시간으로 판단하기에는 애매한데"
- 타당. 26h 창은 "쓰인다"는 증명하지만 "안 쓰인다"는 증명 못 함(주간·월간 배치, 저빈도 관리자 기능 누락).
- 조치: 더 긴 기간 근거 확보 시도 — ①ES/Kibana APM 로그 ②운영 서버 접근로그 ③fluentd/OpenObserve ④정적 호출자 분석(프론트 코드 grep). 실측 가능한 것 전부 동원.

## 14:35 오케스트레이터 → APM 3개월 실측 (사용자 지적 반영)
- ES APM 에 `legacy-service` 트랜잭션 **2026-05-05 ~ 08-09 (3개월)** 존재. 26h 캡처를 대체하는 훨씬 강한 근거.
- **HTTP 엔드포인트 고유 76개 / 총 113,526 호출** (Kafka consumer 트랜잭션은 별도, 여기서 제외)
- 빈도 분포: 1000회+ **18개** · 100~999 **16개** · 10~99 **22개** · **1~9회 20개**
- **26h 캡처(21개)와 3개월(76개) 차이 = 55개**. 사용자 지적이 정확했음 — 26h 는 저빈도 기능을 통째로 놓쳤다.
- 희소(1~9회) 20개 성격: 조직·팀 등록, 채널 추가, 엑셀 다운로드, JWT 발급, 공개병원 조회, 관리자 크롤 요청 등 **관리자·설정성 기능**. 빈도는 낮지만 없으면 업무가 막히는 유형.
- 결론 수정: "147개 중 21개(14%)만 쓴다" → **"76개(52%)가 3개월 내 실사용, 71개는 3개월간 호출 0"**
