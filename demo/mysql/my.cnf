[mysqld]
# mocking-box requirements: ROW-format binlog with full row images & metadata
binlog_format = ROW
binlog_row_image = FULL
binlog_row_metadata = FULL
# carries the original SQL text (incl. /* rid=... */ comments) next to row events
binlog_rows_query_log_events = ON
